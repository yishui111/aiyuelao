# 微聊（仿微信 APP）· 部署与双机测试指南

> 后端：`wechat-backend`（端口 3002，MySQL 持久化）｜手机端：`wechat-app\wechat-release.apk`（约 49KB）｜手表端：`watch-app\wechat-watch-release.apk`（约 33KB）
> 手机端和手表端与后端共用同一套账号、同一个数据库——同一个手机号登录，手机和手表消息同步。

---

## 一、这套服务是什么

在「AI月老」原服务器基础上新增的仿微信后台，微信的核心功能都有接口、都落库：

- **登录注册**：手机号 + 验证码（开发模式固定 `123456`），新手机号登录即注册
- **通讯录**：搜手机号/微信号加好友 → 验证消息 → 通过/拒绝，备注、拉黑、删除
- **聊天**：单聊 + 群聊，文字/表情/图片/拍摄/语音（按住说话）/文件/位置/撤回（2 分钟内），未读数、置顶、免打扰
- **群管理**：建群、拉人、踢人、退群、群名、群公告（群主退出自动转让）
- **位置**：APP 每 8 秒上报 GPS；聊天里可「发送位置」，可「共享实时位置」（双方实时看对方坐标和距离）；发现页有「附近的人」
- **朋友圈**：发图文（最多 9 张）带位置、点赞、评论（可回复）、删除、个人主页
- **实时推送**：消息/好友申请/共享位置更新全部走 WebSocket，无需轮询
- **数据库**：MySQL 库 `wechat`，13 张表（用户/好友/会话/消息/位置/朋友圈），详见 `wechat-backend\README.md`

接口文档：`wechat-backend\README.md`（全部 REST + WebSocket 事件清单）。

## 二、启动服务（电脑上）

1. 双击根目录「启动AI月老.bat」（会拉起 MySQL 3306 + 微聊后端 3002，其余服务可开可不开），或只起这两个：

```
netstat -ano | findstr ":3306 :3002"     # 都有 LISTENING 即在线
curl http://127.0.0.1:3002/api/health    # 返回 {"ok":true,...}
```

2. 后端挂了会由看门狗自动拉起（每 30 秒巡检，日志 `logs\watchdog.log`）。

## 三、装到两台手机测试（核心目标：两台手机互发消息 + 互看位置）

1. **防火墙放行 3002**（管理员 CMD 执行一次即可；不做这步手机一定连不上）：

```bat
netsh advfirewall firewall add rule name="微聊后端(3002)" dir=in action=allow protocol=TCP localport=3002
```

2. **查电脑局域网 IP**：`ipconfig` 看「无线局域网适配器 WLAN」的 IPv4，形如 `192.168.x.x`。

3. **安装 APP**：把 `wechat-app\wechat-release.apk` 分别拷到两台手机安装（允许"安装未知应用"），或 USB 调试：

```
tools\android-sdk\platform-tools\adb.exe install -r wechat-app\wechat-release.apk
```

4. **两台手机各注册一个账号**（连同一个 Wi-Fi）：
   - 首页填 `http://<电脑IP>:3002` → 点「测试连接并进入」提示连接成功
   - 各填**不同手机号** → 「获取验证码」→ 验证码填 `123456` → 登录即注册
   - 登录后先到「我」→ 个人信息，改个昵称方便辨认

5. **互加好友**：A 手机「通讯录」→「新的朋友」旁的 ➕ 或微信页右上角 ➕ →「添加朋友」→ 搜 B 的**手机号** → 添加 → 发送申请；B 手机「通讯录」→「新的朋友」→ 接受。

6. **互发消息**：A 进 B 的资料页 → 「发消息」→ 发文字/图片/语音。B 应实时收到（有未读红点与提示）。

7. **互看位置**：
   - **持续定位**：登录后 APP 每 8 秒自动上报 GPS（首次需允许定位权限，建议窗边或户外完成首帧定位）；
   - **看对方最新位置**：聊天页/资料页 → 「查看对方位置」→ 雷达图显示双方相对方位和距离；
   - **实时位置共享（微信同款）**：聊天页 ➕ → 「共享实时位置」→ 对方点开聊天气泡里的绿色卡片加入 → 双方坐标每 3 秒刷新，退出点「退出共享」。

8. **验收标准**：两台手机互发消息秒收 ✓ 互相能看到对方位置和距离 ✓ 朋友圈互赞互评 ✓。

> 常见问题：连不上 = 不在同一 Wi-Fi / 防火墙没放行 3002 / IP 填错（不是 127.0.0.1）；看不到对方位置 = 双方都要登录并允许定位权限，且互为好友；验证码不对 = 固定填 123456。

## 四、手表端（Wear OS 侧载）

APK 无手机专属依赖，Wear OS 2+（API 23+）可装：

```
tools\android-sdk\platform-tools\adb.exe connect <手表IP>:5555
tools\android-sdk\platform-tools\adb.exe install -r watch-app\wechat-watch-release.apk
```

Wear OS 3+ 桌面可能不显示侧载应用，用 adb 拉起：`adb shell am start -n com.weiliao.watch/.MainActivity`

手表端功能：与手机端**同账号登录**（填同一手机号）、聊天列表 + 未读数、查看消息、快捷回复（好的/稍等/在哪/马上到）、发送手表位置、GPS 每 8 秒上报。手表收到的消息手机端同样可见（同一账号多端同步）。第一版按方屏设计，圆屏可用但未做圆屏优化。

## 五、改了代码怎么重新打包

```
powershell -ExecutionPolicy Bypass -File D:\xm\aiyuelao\wechat-app\build-apk.ps1    # 手机端
powershell -ExecutionPolicy Bypass -File D:\xm\aiyuelao\watch-app\build-apk.ps1     # 手表端
```

全自动 8 步（aapt2 → javac → d8 → 打包 → 对齐 → 签名），无需 Gradle/Android Studio。

**已知坑（脚本已规避，改代码时留意）：**
- JDK21 javac 编译的嵌套类必须全部 `static`，且不能实现泛型回调接口（如 `ValueCallback`），否则 d8 NPE——返回退出统一走 `AndroidBridge.exitApp()` 桥；
- aapt2 在 Windows 打 assets 会用反斜杠路径，assets 由脚本经 jar 以正斜杠补装；
- PowerShell 脚本必须带 UTF-8 BOM，否则中文被按 GBK 误读解析失败；
- `wechat-app\app\assets\www\` 里 `socket.io.min.js` 是本地打包的（来自 wechat-backend 依赖），改后端 socket 协议无需更新它，升级 socket.io 版本时才需要重新拷贝。

## 六、后端自测（不用手机也能回归）

```
cd D:\xm\aiyuelao\wechat-backend
npm test        # 模拟两个用户走完：登录→加好友→单聊→撤回→位置→实时共享→群聊→朋友圈→拉黑，49 项断言
```
