# 新机部署指引(AI月老)

> 目标:新电脑 clone 后,按下面 4 步把全家桶跑起来。所有子项目代码都在自己的仓库里,
> 端口分配:ANL 后端 3001 / ANL 前端 5176 / GlowMeet 后端 8013 / GlowMeet 前端 3000 /
> 匹配引擎 8014 / Shidduch 后端 8015 / Shidduch 前端 5174 / wang 后端 8123 / wang 前端 5175 /
> MySQL 3306 / MongoDB 27017。

## 1. 克隆本仓库 + 拉取子项目

```bat
git clone <本仓库地址> aiyuelao
cd aiyuelao
clone-subprojects.bat
```

## 2. 准备 tools\(免安装的 MySQL / MongoDB / JDK)

start-all.sh 从 `tools\` 目录启动数据库,目录不入库,需要手动准备:

| 目录 | 下载 |
|---|---|
| tools\mongodb-extracted\ | https://www.mongodb.com/try/download/community (zip 版解压) |
| tools\mysql-extracted\ | https://dev.mysql.com/downloads/mysql/ (zip 版解压) |
| tools\jdk21-extracted\ | https://adoptium.net (JDK 21 zip 解压) |

(如果老机器还在,直接把 `tools\` 整个目录拷过来最快。)

## 3. 子项目初始化

| 子项目 | 初始化 | 说明 |
|---|---|---|
| projects\ai-powered-matching-algorithm | `python -m venv .venv && .venv\Scripts\pip install -r requirements.txt`(若有) | Python 3.10+ |
| projects\shidduch-app\backend | `python -m venv .venv && .venv\Scripts\pip install -r requirements.txt` | 后端 FastAPI |
| projects\shidduch-app\frontend | `npm install` | 前端 Vite |
| projects\ANL | `npm install`(backend 与 frontend 各自) | React 应用 |
| projects\GlowMeet | 无需(后端 exe 已随仓库) | 需要改动时用 Go 重编 |
| projects\wang-ai-agent | 需 JDK21 + Maven: `mvn package` | 源码仓库: github.com/Uwilldoit/wang-ai-agent |

## 4. 启动

Git Bash 里执行 `bash start-all.sh`,会依次拉起 MongoDB → MySQL → 各子项目,
结尾自动做健康检查。停止:`bash stop-all.sh`。
