# AI月老 — 开源项目整合部署报告 & 实施方案优化建议

> 部署日期：2026-09-05 ｜ 环境：Windows 10 ｜ 方式：**全部原生进程，未使用 Docker**

---

## 一、部署结果总览

方案中列出的 6 个开源项目已全部拉取到 `D:\xm\aiyuelao\projects\`，其中 5 个已完成部署并验证，1 个（WeClone）按方案定位仅作参考（训练数字分身需要 GPU，暂不部署）。

| 服务 | 端口 | 访问地址 | 状态 | 验证结果 |
|---|---|---|---|---|
| **ANL 后端**（附近的人+匹配主框架） | 3001 | http://localhost:3001/api/health | ✅ | 登录/附近的人/资料接口全链路通过 |
| **ANL 前端**（React+Vite PWA） | 5173 | http://localhost:5173 | ✅ | 页面 200 |
| **AI匹配引擎**（ai-powered-matching-algorithm） | 8001 | http://127.0.0.1:8001/docs | ✅ | 真实返回带分数的匹配列表 |
| **GlowMeet 后端**（Go，定位匹配参考） | 8000 | http://localhost:8000/health | ✅ | /health OK，OAuth 端点可签发 state |
| **GlowMeet 前端** | 3000 | http://localhost:3000 | ✅ | 代理链路通 |
| **Shidduch 后端**（AI 匹配管道 FastAPI） | 8010 | http://127.0.0.1:8010/docs | ✅ | 登录返回 JWT，MongoDB 就绪 |
| **Shidduch 前端**（React+TS） | 5174 | http://localhost:5174 | ✅ | 页面 200 |
| **wang-ai-agent 后端**（AI恋爱军师 Spring Boot） | 8123 | http://localhost:8123/api/doc.html | ✅ | 健康检查 OK，接口文档可访问 |
| **wang-ai-agent 前端**（Vue3） | 5175 | http://localhost:5175 | ✅ | 页面 200 |
| MongoDB（Shidduch 存储，限缓存 256MB） | 27017 | 仅本机 | ✅ | |
| MySQL 8（wang-ai-agent 对话记忆） | 3306 | 仅本机，root/123456，库 chat | ✅ | chat_message 表已建 |

**资源占用：全部服务合计约 2.6 GB 内存**（node×10 ≈ 760MB、python×7 ≈ 415MB、java 289MB、mongod 817MB、mysqld 414MB、glowmeet 10MB），系统仍剩 12.6GB 可用。

## 二、一键启停

```bash
bash D:/xm/aiyuelao/start-all.sh   # 一键拉起全部 11 个服务并自检
bash D:/xm/aiyuelao/stop-all.sh    # 一键停止
```

日志统一在 `D:\xm\aiyuelao\logs\`，便携工具链在 `tools\`，数据库文件在 `data\`。

## 三、测试账号

| 系统 | 账号 |
|---|---|
| ANL | 任意手机号登录，验证码固定 `123456` |
| Shidduch | `admin / admin123`（seed_demo 还有 demo 账号） |
| wang-ai-agent | 打开前端即用（对话需先填 DashScope key） |
| GlowMeet | 需 X(Twitter) 开发者 key，本地仅作参考 |

## 四、为跑起来所做的最小修改（全部可回滚）

原则：**能不改就不改，只动配置；必须动的都是"注释/可选注入"级别**。

### wang-ai-agent（核心矛盾：原设计必须同时跑 PostgreSQL+pgvector 和 MCP 服务才能启动）
1. `rag/PgVectorVectorStoreConfig.java`：注释 `@Configuration`——本地无 pgvector，该配置启动时就会对数据库执行 PG 方言建表 SQL，不注释起不来。**生产启用 pgvector 时恢复此注解即可。**
2. `rag/LoveAppVectorStoreConfig.java`：恢复 `@Configuration`（启用项目自带的内存知识库 SimpleVectorStore）+ 文档加载包 try/catch（未配 DashScope key 时不阻塞启动）。
3. `app/LoveApp.java`：RAG advisor 由 `pgVectorVectorStore` 切换到 `loveAppVectorStore`（本来就是代码里注释着的备选方案）；`pgVectorVectorStore`、`ToolCallbackProvider` 改为 `@Autowired(required=false)`。
4. `agent/ToolCallAgent.java`：`ToolCallbackProvider` 同上改可选（配合 `application-local.yml` 里关闭 MCP 客户端，省一个常驻 JVM）。
5. 新增 `src/main/resources/application-local.yml`：DashScope 占位 key、关闭 MCP 客户端。数据源不用改——本机 MySQL 按项目默认的 root/123456 + chat 库搭建。

### 端口冲突类（纯配置，各 1-2 行）
- `shidduch-app/frontend/vite.config.ts`：5173→**5174**、API 代理 8000→**8010**（5173 被 ANL、8000 被 GlowMeet 占用）。
- `wang-ai-agent-frontend/vite.config.js`：5173→**5175**。
- ANL：零代码改动，仅新增 `.env` / `backend/.env.local`（精简后端入口要求 JWT_SECRET/TURN_SECRET）。
- GlowMeet：零代码改动，仅占位环境变量启动。

## 五、需要你补充的 API Key（不填不影响启动，只影响对应功能）

| 项目 | Key | 用途 | 填的位置 |
|---|---|---|---|
| wang-ai-agent | 阿里云百炼 DashScope key | AI 对话/RAG | `wang-ai-agent/src/main/resources/application-local.yml` |
| Shidduch | OpenAI key | 资料向量化(embedding) | `shidduch-app/backend/.env` |
| Shidduch | Anthropic key | 匹配精排(0-10评分+解释) | 同上 |
| GlowMeet | X 开发者 key | OAuth 登录（国内需改造，见下文） | 启动环境变量 |
| ANL | 无需 | 内存模式全功能可用 | — |

## 六、实施方案评审结论与优化建议

方案整体判断是**对的**：选型思路（现成项目拼积木、ANL 做壳、服务器算分、AI 破冰真人收尾）和成本意识都很好，列出的 6 个仓库经逐一验证**全部真实存在**（weclone 的准确地址是 `xming521/WeClone`，方案没写全）。以下是这次实机部署 + 通读源码后发现的优化点，按重要性排序：

### 1. 技术栈收敛（最重要）
现在整合的是 5 种技术栈：Node/Express + FastAPI×2 + Spring Boot + Go，存储有 Supabase PG / MongoDB / MySQL / pgvector / 内存 5 套。一台 4核8G 同时养 5 个存储 + 4 个后端运行时，方案 116 行的成本估算撑不住。
**建议**：终态收敛到 "PostgreSQL(含pgvector) 一个存储 + 两个后端"——ANL(Node) 做主壳和全部业务表；匹配管道把 Shidduch 的 `embeddings.py + matcher.py` 抽成独立 FastAPI 微服务（它的管道本来就封装在 services 层，抽出成本低）；wang-ai-agent 的 RAG 用 pgvector（它原生设计如此）。GlowMeet 只参考"发光圆点"的可视化交互，不部署（Go 栈额外养一个运行时不值）。MongoDB 在数据量小的时候可由 PG JSONB 顶替。**本次部署先按原样整合未动架构，收敛可作为第二阶段的重构目标。**

### 2. AI 供应商统一到国内一家
Shidduch 用 OpenAI embedding + Claude 评分，wang-ai-agent 用 DashScope qwen，两套 key 两种 SDK，且国内服务器访问 OpenAI/Anthropic 本身不稳定。DashScope 的 `text-embedding-v3` + `qwen-max` 完全能覆盖"向量化→余弦相似度→LLM评分"管道。Shidduch 的模型调用都封装在 `services/embeddings.py` 和 `services/matcher.py`，换 SDK 的改动面很小。省钱、稳定、合规三赚。

### 3. 第二阶段"位置触发匹配"要重新设计，否则成本失控
方案 86 行"服务器持续追踪位置，条件满足时自动触发"有两个坑：
- **定位策略**：持续 GPS 费电且隐私敏感。建议改为地理围栏/geohash 网格变化时才上报（进入新网格才算一次位置更新）。
- **触发成本**：Shidduch 管道每对候选人要调一次 LLM 评分。若每次位置更新都全量跑，1000 日活能把 API 费用放大百倍。建议两级漏斗：位置更新只触发**零成本硬筛选**（geohash 距离 + 年龄性别等硬条件），命中后进**批量精排**（每天 1-2 次或双方都开启"找对象"状态时才实时算）。方案里 300-800 元/月的 LLM 预算按现设计会明显失真。

### 4. 合规成本必须进预算
婚恋交友是国内强监管品类，方案成本表缺失这块：ICP 备案/EDI、实名认证（手机 OTP 之外需运营商三要素核验）、**AI 生成内容审核**（AI 分身的回复必须接内容安全审核，否则上架和内测都会出事）、位置信息单独授权。建议成本表加一行"合规与内容审核"，这属于不能省的固定项。

### 5. 时间线可以从 16 周压到 ~12 周
- 手表/戒指阶段（2 周）建议移到最后甚至砍掉：震动推送不改变"匹配转化率"这个核心假设，先验证假设再说。
- 一二阶段可部分并行：ANL 骨架（4周）和匹配引擎独立部署（本次已验证可独立跑通）天然解耦。

### 6. 内测指标补充过程指标
三个北极星指标选得好，但"AI 破冰有效率>30%"不达标时分不清是匹配不准还是 AI 答得差。建议加两个过程指标：AI 话术被真人采纳率、每对平均 AI 对话轮次。

### 7. 几处与实际的出入（部署实勘）
- ANL 的 `npm run dev` 跑的仍是全量 server.js（需要真 PG）；**开箱即跑的内存模式入口是 `server-minimal.js`**（本次采用）。切生产 PG 时按 `supabase/schema.sql` → `backend/db/migrations` 顺序执行即可。
- Shidduch 要求 **Python ≥3.11**（本机 3.10 不够，已用 uv 独立装 3.12）。
- wang-ai-agent 仓库不含 `application-local.yml`，且 README 未提 MCP 服务必须先跑在 8127——不跑 MCP 就得像我这样把注入改可选。
- GlowMeet 实际是 **Go** 技术栈（方案未注明），且登录强绑定 X(Twitter)，国内产品只能参考其匹配可视化，登录需换手机号/微信。

## 七、已知限制

- wang-ai-agent 对话/RAG：等填真实 DashScope key 后即通，当前接口层已验证可达。
- Shidduch 匹配管道（match-run）：等填 OpenAI/Anthropic key；登录、候选人管理、数据库读写均已可用。
- GlowMeet：无真实 X key 时无法走完登录，作为定位匹配的参考实现。
- WeClone（`xming521/WeClone`）：仅拉取备查，训练需要 GPU，未部署。
