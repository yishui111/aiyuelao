# 两台手机测试步骤（照单执行）

> 目标：两台手机安装 APP 后，**能互发消息、能互相看到对方位置**。
> 测试日期：＿＿＿＿＿＿　测试人：＿＿＿＿＿＿　电脑 IP：＿＿＿＿＿＿（第三步填）
> 详细背景看《微聊部署与双机测试文档.md》，本文档是照着勾的执行单。

---

## 一、测试前准备（电脑上，约 5 分钟）

- [ ] **1. 双击根目录「一键启动所有服务.bat」**，等窗口自检全部出现 `OK`（约 40 秒）。
      必须在线的两个：`ANL后端 3001` 不需要也行，**`微聊后端 3002` 必须是 OK**。
      本窗口别关（最小化即可），服务在窗口背后运行。
- [ ] **2. 防火墙放行 3002（只需做一次，做过可跳过）**：管理员 CMD 执行
      `netsh advfirewall firewall add rule name="微聊后端(3002)" dir=in action=allow protocol=TCP localport=3002`
      （「一键启动所有服务.bat」以管理员运行时会自动放行，看窗口提示）
- [ ] **3. 查电脑局域网 IP**：`ipconfig` → 「无线局域网适配器 WLAN」→ IPv4，形如 `192.168.x.x`，记下来。
- [ ] **4. 把 `wechat-app\wechat-release.apk` 拷到两台手机**（微信文件传输助手/QQ/数据线均可），
      各自安装，允许「安装未知应用」。
- [ ] **5. 两台手机连接同一个 Wi-Fi**（能访问电脑所在局域网的那个）。

## 二、注册登录（两台手机各做一遍）

- [ ] **T1 连服务器**：打开 APP → 第一栏填 `http://<电脑IP>:3002` → 点「测试连接并进入」→ **预期：提示 连接成功 ✓**（填错会提示连接失败）
- [ ] **T2 登录注册**：填一个 11 位手机号（两台手机**用不同号码**）→「获取验证码」→ 验证码自动填 **123456** → 「登录 / 注册」→ **预期：进入主界面，底部有 微信/通讯录/发现/我 四个 tab**
- [ ] **T3 改资料**：「我」→ 点头像那行 → 改昵称（如 手机A / 手机B）、头像、签名 → **预期：保存后「我」页显示新昵称**

## 三、加好友

- [ ] **T4 搜索并申请**：手机A「微信」页右上角 ➕ →「添加朋友」→ 输入手机B的**手机号** → 搜索 → 点「添加」→ 验证消息默认 →「发送申请」→ **预期：提示 申请已发送**
- [ ] **T5 通过申请**：手机B「通讯录」→「新的朋友」→ 应看到A的申请（有红点角标）→ 点「接受」→ **预期：通讯录列表出现A**；手机A应收到「已通过」的提示

## 四、聊天

- [ ] **T6 互发文字**：A 进 B 资料页 →「发消息」→ 发"你好" → **预期：B 在 1 秒内实时收到，B 的会话列表有未读红点**；B 回复一条 → A 实时收到
- [ ] **T7 发图片**：聊天 ➕ →「相册」选一张图 → **预期：对方收到并能看到图片**
- [ ] **T8 发语音**：聊天左侧话筒图标 →「按住 说话」松手 → **预期：对方收到语音气泡，点击能播放**（首次要允许麦克风权限；个别老机型不支持会提示，跳过即可）
- [ ] **T9 撤回**：发出一条消息后 2 分钟内，点该消息的撤回入口 → **预期：双方都看到"撤回了一条消息"**
- [ ] **T10 未读与置顶**：给B连发 3 条，B 不点开 → **预期：B 列表显示未读数 3**；B 点开后清零

## 五、位置（核心）

> 前提：两台手机都允许了定位权限（首次进 APP 会弹），最好在窗边或户外完成首次定位；APP 登录后每 8 秒自动上报位置。

- [ ] **T11 发送位置**：聊天 ➕ →「位置」→ **预期：对方收到绿色底图的位置卡片，点开能看到坐标**
- [ ] **T12 看对方位置**：聊天右上角「···」→ 资料页 →「查看对方位置」→ **预期：雷达图显示对方绿点相对我的方位，下方文字显示两地距离**
- [ ] **T13 共享实时位置（微信同款）**：A 聊天 ➕ →「共享实时位置」→ **预期：进入绿色标题的共享页**；B 在聊天里点开绿色「实时位置共享」卡片加入 → **预期：双方页面都显示两个人的点和距离，约 3 秒刷新一次**；A 拿着手机走动 → **预期：B 屏幕上 A 的点会动**；任一方点「退出共享」结束
- [ ] **T14 附近的人**：「发现」→「附近的人」→ **预期：列表能看到对方（显示距离）**

## 六、朋友圈

- [ ] **T15 发朋友圈**：「发现」→「朋友圈」→ 右上角相机 → 写文字 + 选 1-2 张图 →「发表」→ **预期：对方刷朋友圈能看到**
- [ ] **T16 点赞评论**：对方在朋友圈点「赞」和「评论」→ **预期：发布者收到提示，刷新可见**

## 七、群聊（可选）

- [ ] **T17 建群**：微信页 ➕ →「发起群聊」→ 填群名、勾选对方 → 确定 → **预期：进入群聊**；对方发消息所有人可见；➕ 里可拉人，右上角可看群成员/公告/退群

## 八、手表端（可选）

- [ ] **T18 手表侧载**：`adb connect <手表IP>:5555` 后
      `tools\android-sdk\platform-tools\adb.exe install -r watch-app\wechat-watch-release.apk`
      （Wear OS 3+ 桌面看不到图标时：`adb shell am start -n com.weiliao.watch/.MainActivity`）
- [ ] **T19 手表登录收发**：手表填同一服务器地址，用**手机A同一手机号**登录 → **预期：能看到 A 的会话和消息**；点快捷回复「好的」→ **预期：手机B实时收到**；顶部显示 GPS 状态

## 九、验收判定

| 项目 | 通过标准 | 结果 |
|---|---|---|
| 聊天 | 两台手机互发消息实时可达（T6-T8） | ☐ 通过 ☐ 不通过 |
| 位置 | 能看到对方位置与距离；实时共享双方坐标会动（T11-T13） | ☐ 通过 ☐ 不通过 |
| 其他 | 加好友/朋友圈/群聊正常（T4/T5/T15-T17） | ☐ 通过 ☐ 不通过 |

## 十、常见问题

| 现象 | 处理 |
|---|---|
| 测试连接 ✗ | 两台手机没连同一 Wi-Fi；防火墙没放行 3002；IP 填错（要电脑局域网 IP，不是 127.0.0.1） |
| 一直"定位中…" | 手机定位服务没开；APP 定位权限被拒（设置→应用→微聊→权限）；室内信号弱，去窗边 |
| 看不到对方位置 | 双方都要登录过且上报过位置；必须互为好友；被拉黑也看不到 |
| 附近列表为空 | 对方 1 小时内没上报过位置；或你们相距超过 10 公里 |
| 收不到消息 | 看电脑上 `一键启动所有服务.bat` 窗口自检，微聊后端 3002 是否 OK；挂了看门狗 30 秒内会自动拉起 |
| 服务莫名重启 | 正常，看门狗在保活；日志在 `logs\weiliao-backend.log` |

## 十一、测完收尾

双击根目录「停止AI月老.bat」停止全部服务（含数据库）。
